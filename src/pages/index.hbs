<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="https://glitch.com/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>{{seo.title}}</title>
    
    <!-- Meta tags for SEO and social sharing -->
    <link rel="canonical" href="{{seo.url}}">
    <meta name="description" content="{{seo.description}}">
    <meta property="og:title" content="{{seo.title}}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{seo.url}}">
    <meta property="og:description" content="{{seo.description}}">
    <meta property="og:image" content="{{seo.image}}">
    <meta name="twitter:card" content="summary">

    <!-- Import the webpage's stylesheet -->
    <link rel="stylesheet" href="/style.css" />  
  </head>
  <body>
    <div class="wrapper">
      <div class="content" role="main">
        
        <!-- This is the start of content for our page -->
        <h1 class="title">Kaomoji Generator</h1>
        
        <!-- Add text indicating that we've changed color OR had an error -->
        <p class="kaomoji-info">
          Transform any word in <b>Kaomoji</b>!
        </p>
        <p class="kaomoji-info">
          {{kaomoji}}
        </p>
        
        {{#if error}}
          <p class="kaomoji-info">            
            Hmm. Couldn't make a Kaomoji for <b>"{{word}}"</b>. 
          </p>
          <a href="?randomize=go">Try a random Kaomoji?</a>
        {{/if}}
        
        <div class="kaomoji-form">          
          <!-- The "What's your favorite color?" form sends a POST request to the server
               Check server.js to see how it works -->
          <form class="kaomoji-search" method="post">
              <label for="word">
                
                <!-- We use the handlebars "#if" statement so the form can adapt to its situation -->
                {{#if kaomoji}}
                  Try another word?<br>
                {{else}}
                  What word do you want to transform in kaomoji?<br>
                {{/if}}
                <input id="word" name="word" required="required" type="text">
              </label>
            
            <!-- If the user submits a value through the form 
                 it'll be passed to the server in the request body  -->
            <button type="submit">Submit</button>
          </form>
          
          <!-- This randomize link communicates with the server (server.js) 
               using a querystring and GET request instead of a form POST -->
          Can't decide? <a href="?randomize=go">Use a random Kaomoji!</a>
        </div>
        
        <div class="illo-container">
          <img src="https://cdn.glitch.com/c62efef6-1e75-45cf-b248-afeccdda9477%2Fillustration.svg?v=1618196069568" class="illustration" alt="Node logo" />
        </div>        
      </div>
    </div>
    
    <script>
      document.getElementById("color").addEventListener('keyup', function() {
        console.log("keyup");
        var text = this.value;
        this.value = text.replace(/[^A-Za-z]/g, '');
      });
    </script>
  </body>
</html>
